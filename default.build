<?xml version="1.0"?>
<project name="NAnt Examples" default="rebuild">
	
	<property name="build.folder" value="${directory::get-current-directory()}" />
	
	<include buildfile="build/opencover.build" />
	<include buildfile="build/environment.build" />

	<target name="rebuild" depends="prepare-environment, clean, build, test, test-cpp" />

</project>